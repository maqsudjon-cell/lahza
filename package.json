{
  "name": "lahza",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-08-01 --r2=PHOTOS --port 8788",
    "deploy": "wrangler pages deploy public"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
